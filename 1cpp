#include <iostream>
using namespace std;

int main()
{
    int number , product , counter ;
    cout<< "enter a int number:";
    cin>> number;
    counter =1;
    while (number>0){
        product = z * counter;
        cout<< "product is:" << product << endl;
        if(z<0){
            cout<< "the number is negative";
        }
        if(number<0){
            break;
        }

    }
return 0;
}
